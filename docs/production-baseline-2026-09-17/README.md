# 本番稼働確認版の保全 — 2026-09-17

このコミットは現行本番を基準にした保全であり、リファクタリングやバージョンアップではない。本番・DB・コンテナーは変更していない。タグ `production-baseline-2026-09-17` を今後の比較基準とする。「最新」は調査時点の本番稼働版を指し、未検証のMac変更やサーバー作業ツリーを最新版として採用していない。

秘密鍵・Firebase Admin資格情報・DB・証明書は含めない。起動には既存の資格情報と環境設定が別途必要。Macの既存作業ツリーとサーバー作業ツリーは変更していない。

## 比較資料

`source-hashes.json` に各ファイルのSHA-256を記録。production列の欠落はその取得方法では観測できなかったことを意味し、実行時に不存在とは限らない。GitHubの更新前との差分は本コミットのdiffで確認できる。

## 採用元と重要差分

- 稼働中 `lta-webapp:/usr/share/nginx/html` を `served-web-assets.tar.gz` として完全保全。各ファイルのハッシュはserved-assets-sha256.json。イメージID `sha256:2bf5b598ea86a915b56f5b3dee03c0ec071920fdd31c9329efbeb52564a205d4`。
- GitHub・サーバーの基底HEAD: cea6632f57f6ef03510f21f3f72cea8c5441cb15。
- ソースマップから元Vue/JSソース29ファイルを復元。コンパイル済みrender関数ではなく元のVue SFCを使用。画面コンポーネント・ルーター・サービス等27ファイルはサーバーとMacの両方に一致。
- `src/http-common.js`: 本番はynu-lta.dev.alchembright.comのAPI。Macも同一。サーバーは旧ルンド大学のAPI接続先。今回ドメイン変更をせず本番を保全。
- `src/main.js`: 本番はynu-lta-dev Firebase設定。サーバーは旧lang-track-app設定。Macは同じYNUプロジェクトだがdatabaseURLの末尾スラッシュ・並び・空白が相違。本番の原文を採用。
- nginx設定もコンテナーから取得。旧ドメインのserver_name、HTTP、X-Forwarded-Forの誤記等は修正せず現状記録。修正は後続フェーズ。
- package.json、Dockerfile等はソースマップから復元できないためサーバー作業ツリー由来。これらが配信資産の生成時に使われたことまでは証明できない。生成時のlockfileも未取得のため、再ビルドのバイト一致を保証しない。

## 検証・制限

指定アカウントで本番ログイン、Users一覧・Assignments検索期間画面を閲覧できた。保全アーカイブは配信資産とハッシュ照合済み。ソースマップ由来の29ファイルは本番配信時のソースとして確認。依存を新規解決するビルドは実施していない。配信済み成果物の保存と、将来の再現可能なビルド整備を区別する。
